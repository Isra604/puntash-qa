# Codex Quick Start

Open Codex in the target repository/workspace and provide:

```text
Read .comprehensive-qa/START_HERE.md and .comprehensive-qa/AGENT_INSTRUCTIONS.md in full. Read .comprehensive-qa/state/QA_DOCTOR.json if present. Verify all Doctor hints from repository evidence. Perform Phase 0 and Discovery first, then build the Project QA Profile and execute the mapped 25-gate QA cycle within current authority boundaries. Preserve evidence and never infer PASS from missing capabilities.
```


v2.1 rule: read `state/OWNER_POLICY.json` before remediation. If it is unconfigured, remain REPORT_ONLY and ask the owner about scheduling/remediation preferences. Before every automatic product mutation, require an ALLOW from the installed `authorize-change` tool. Scheduling never grants remediation authority.
